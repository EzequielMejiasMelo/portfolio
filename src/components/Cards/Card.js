import React from "react";

const Card = (card) => {
    return (
        <div className="card w-100 h-100">
            <img className="card-img-top image-card" alt="Screenshot of the application"></img>
        </div>
    )
};

export default Card;